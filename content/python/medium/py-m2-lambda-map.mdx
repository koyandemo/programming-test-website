---
id: "q_5Wr3Tn7Y"
questionType: "MULTIPLE_CHOICE"
title: "Lambda with Map"
description: "What is the result of list(map(lambda x: x**2, [1, 2, 3]))?"
difficulty: "MEDIUM"
category: "Python"
options:
  - "[1, 2, 3]"
  - "[1, 4, 9]"
  - "[2, 4, 6]"
  - "[1, 8, 27]"
correctAnswer: 1
---

# Lambda with Map

Lambda functions are often used with map() for transformations.

The `map()` function applies the lambda function `lambda x: x**2` to each element in the list [1, 2, 3], squaring each number. This results in [1, 4, 9].
\`\`\`
