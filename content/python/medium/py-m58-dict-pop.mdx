---
id: "q_py_m58"
questionType: "TRUE_FALSE"
title: "Dict Pop"
statement: "dict.pop(key, default) returns and removes the value for key, or returns default if key is not found."
difficulty: "MEDIUM"
category: "Python"
correctAnswer: true
explanation: "pop(key, default) safely removes and returns a value. Without default, it raises KeyError if key doesn't exist."
---

# Dict Pop

pop() provides a safe way to remove dictionary items with a fallback value.
