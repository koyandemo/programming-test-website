---
id: "q_2Zm5Pk7R"
questionType: "MULTIPLE_CHOICE"
title: "Enumerate Function"
description: "What does enumerate(['a', 'b', 'c']) return when converted to a list?"
difficulty: "MEDIUM"
category: "Python"
options:
  - "['a', 'b', 'c']"
  - "[(0, 'a'), (1, 'b'), (2, 'c')]"
  - "[('a', 0), ('b', 1), ('c', 2)]"
  - "{'a': 0, 'b': 1, 'c': 2}"
correctAnswer: 1
---

# Enumerate Function

Understanding how enumerate() adds indices to iterables.

The `enumerate()` function returns an iterator of tuples, where each tuple contains an index (starting from 0 by default) and the corresponding value from the iterable. This is useful for getting both index and value in loops.
\`\`\`
