---
id: "q_py_m20"
questionType: "TRUE_FALSE"
title: "Isinstance Check"
statement: "isinstance() can check against multiple types using a tuple: isinstance(x, (int, float))."
difficulty: "MEDIUM"
category: "Python"
correctAnswer: true
explanation: "isinstance() accepts a tuple of types as the second argument and returns True if the object is an instance of any of those types."
---

# Isinstance Check

The isinstance() function supports checking multiple types at once by passing them as a tuple.
