---
id: "q_py_m31"
questionType: "MULTIPLE_CHOICE"
title: "Filter Function"
description: "What does list(filter(lambda x: x > 0, [-1, 0, 1, 2])) return?"
difficulty: "MEDIUM"
category: "Python"
options:
  - "[1, 2]"
  - "[-1, 0, 1, 2]"
  - "[0, 1, 2]"
  - "[-1, 1, 2]"
correctAnswer: 0
---

# Filter Function

filter() applies the lambda function to each element and keeps only those where the function returns True. Here, only 1 and 2 are greater than 0.
