---
id: "q_py_m32"
questionType: "TRUE_FALSE"
title: "Map Function"
statement: "map() returns a list of results after applying a function to each item."
difficulty: "MEDIUM"
category: "Python"
correctAnswer: false
explanation: "In Python 3, map() returns a map object (iterator), not a list. You need to convert it to a list explicitly: list(map(...))."
---

# Map Function

map() returns an iterator in Python 3, not a list. Use list() to convert it if needed.
