---
id: "q_py_e52"
questionType: "TRUE_FALSE"
title: "List Extend Method"
statement: "The extend() method adds all elements from another list to the end of the current list."
difficulty: "EASY"
category: "Python"
correctAnswer: true
explanation: "extend() iterates over the iterable and appends each element, unlike append() which adds the entire object."
---

# List Extend Method

Use `extend()` to merge lists and `append()` to add a single element.
