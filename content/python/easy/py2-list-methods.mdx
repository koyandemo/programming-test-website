---
id: "py2"
questionType: "MULTIPLE_CHOICE"
title: "Python List Methods"
description: "Which method adds an element to the end of a Python list?"
difficulty: "EASY"
category: "Python"
options:
  - "add()"
  - "append()"
  - "insert()"
  - "push()"
correctAnswer: 1
---

# Python List Methods

Understanding list methods is fundamental to working with Python collections.

The `append()` method adds a single element to the end of a list. The `insert()` method can add at any position, while `add()` and `push()` are not list methods in Python.
\`\`\`
