---
id: "q_5Rp8Lm2N"
questionType: "MULTIPLE_CHOICE"
title: "Python Descriptors"
description: "Which method is called when accessing an attribute through a descriptor?"
difficulty: "HARD"
category: "Python"
options:
  - "__get__()"
  - "__set__()"
  - "__delete__()"
  - "__getattribute__()"
correctAnswer: 0
---

# Python Descriptors

Descriptors are objects that define how attribute access is handled. The `__get__()` method is called when the attribute is accessed, `__set__()` when assigned, and `__delete__()` when deleted. Properties, methods, and classmethods are all implemented using descriptors.
\`\`\`
