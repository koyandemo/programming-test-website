---
id: "q_x7v4h9n2"
questionType: "MULTIPLE_CHOICE"
title: "Array Method Mutation"
description: "Which of these array methods does NOT mutate the original array?"
difficulty: "MEDIUM"
category: "JavaScript"
options:
  - "push()"
  - "splice()"
  - "map()"
  - "sort()"
correctAnswer: 2
---

# Array Method Mutation

The `map()` method creates and returns a new array without modifying the original, while push(), splice(), and sort() all mutate the original array.
\`\`\`
