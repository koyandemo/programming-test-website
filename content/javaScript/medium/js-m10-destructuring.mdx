---
id: "q_c1r6m9n4"
questionType: "TRUE_FALSE"
title: "Destructuring Default Values"
statement: "In destructuring, default values are only used when the property is undefined, not when it's null."
difficulty: "MEDIUM"
category: "JavaScript"
correctAnswer: true
explanation: "Default values in destructuring are only applied when the value is undefined. null is considered a valid value and won't trigger the default."
---

# Destructuring Default Values

When destructuring with default values, the default is only used if the value is strictly `undefined`. `null`, `0`, `false`, and empty strings don't trigger defaults.
\`\`\`

- ... and 40 more files ...
