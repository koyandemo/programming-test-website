---
id: "js6"
type: "MULTIPLE_CHOICE"
title: "JavaScript Promises"
description: "Which method is used to handle errors in a Promise chain?"
difficulty: "MEDIUM"
category: "JavaScript"
options:
  - "then()"
  - "catch()"
  - "finally()"
  - "reject()"
correctAnswer: 1
---

# JavaScript Promises

This question tests your understanding of Promise error handling in JavaScript.

The `catch()` method is specifically designed to handle errors (rejections) in a Promise chain. It catches any errors that occur in the preceding `then()` calls.
\`\`\`
